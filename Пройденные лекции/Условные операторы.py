#Задача 1 (Логические операции)
#Напиши условие, которое проверяет, что переменная age больше 18 и переменная access равна True.
#Выведи "Доступ разрешён", если условие выполняется.
#(Переменные уже заданы: age = 20, access = True)
age = 20
access = True
if age > 18 and access == True :
    print("Доступ разрешен")

#Задача 2 (Оператор elif)Напиши код, который проверяет значение переменной time (в часах) и выводит:
#"Утро", если time от 5 до 11 (включительно),
#"День", если time от 12 до 17,
#"Вечер", если time от 18 до 22,
#"Ночь" во всех остальных случаях.
time = 23
if 5 <= time <= 11:
    print("Утро")
elif 12 <= time <= 17:
    print("День")
elif 18 <= time <= 22:
    print("Вечер")
else:
    print("Ночь")

#Задача 3 (Тернарный оператор)
#Дана переменная is_rain (логическое значение — True или False).
#Напиши одной строкой код, который:
#присваивает переменной action строку "Беру зонт", если is_rain == True,
#иначе присваивает "Иду без зонта".
is_rain = False
action = "Беру зонт" if is_rain else "Иду без зонта"
print(action)

#Вот задача 4 — теперь на комбинацию логических операторов (and, or, not).
#Напиши программу, которая проверяет, может ли пользователь получить скидку в магазине. Скидка даётся, если:
#возраст (age) от 12 до 18 лет (включительно) ИЛИ
#если покупатель — студент (is_student = True) И его возраст меньше 65 лет.
#Выведи "Скидка доступна", если условия выполняются, иначе — "Скидка не доступна".
age = 25
is_student = False
if (12 <= age <= 18) or (is_student and age < 65):
    print("Скидка доступна")
else :
    print("Скидка не доступна")


#Вот задача 5 на оператор not — инвертирование условий.
#В кинотеатре есть ограничение: дети младше 10 лет (age < 10) не могут смотреть фильм ужасов (is_horror = True).
#Напиши условие, которое проверяет, можно ли допустить зрителя, и выводит:
#"Доступ разрешён" — если возраст ≥ 10 ИЛИ фильм не ужасов,
#"Доступ запрещён" — во всех остальных случаях.
age = 8
is_horror = True
if age >= 10 or not is_horror:
    print("Доступ разрешён")
else:
    print("Доступ запрещён")

#задача 6 на вложенные if
#Напиши программу для автоподбора одежды по погоде, которая проверяет:
#Если идет дождь (is_rain = True), советует "Надеть плащ",
#Если дождя нет:
#При температуре выше 20°C (temp > 20) советует "Надеть футболку",
#Иначе советует "Надеть свитер".
is_rain = True
temp = 18
if is_rain:
    print("Надеть плащ")
else:
    if temp > 20:
        print("Надеть футболку")
    else:
        print("Надеть свитер")


#Задача 8 (Вложенные if)Напиши программу для кинотеатра, которая проверяет, можно ли продать билет:
#Если фильм не 18+ (is_adult = False), билет продаётся без ограничений,
#Если фильм 18+, то:
#Проверить возраст (age >= 18),
#И наличие документа (has_id = True).
#Выведи:
#"Билет продан", если условия выполнены,
#"Отказ: не подходит возраст" (если возраст < 18),
#"Отказ: нет документа" (если возраст ≥18, но нет документа).
age = 20
is_adult = True
has_id = False
if not is_adult:
    print("Билет продан")
elif is_adult:
    if age >= 18 and has_id:
        print("Билет продан")
    elif age >= 18 and not has_id:
        print("Отказ: нет документа")
    else:
        print("Отказ: не подходит возраст")

#Задача 9: Условие:В магазине действуют акции:
#Скидка 10% даётся, если:
#Покупатель — студент (is_student = True) ИЛИ пенсионер (is_pensioner = True),
#И при этом сумма покупки (purchase_amount) больше 1000 рублей.
#Напиши условие, которое проверяет эти правила и выводит:
#"Скидка 10% применена", если условия выполнены,
#"Скидка не доступна" — в остальных случаях.

is_student = False
is_pensioner = False
purchase_amount = 1500
if (is_student or is_pensioner) and purchase_amount > 1000:
    print("Скидка 10% применена")
else:
    print("Скидка не доступна")

#Задача 10:Ты пишешь программу для доступа в секцию коворкинга. Правила:
#Доступ разрешён, если:
#Посетитель — студент (is_student = True) И у него есть пропуск (has_pass = True),
#ИЛИ если он гость организатора (is_guest = True) И время визита до 20:00 (current_hour < 20),
#ИЛИ если он платит на месте (pay_on_site = True) И есть свободные места (free_seats > 0).
#"Добро пожаловать!", если условия выполнены,
#"Доступ запрещён" — в остальных случаях.
is_student = False
has_pass = False
is_guest = True
current_hour = 21
pay_on_site = False
free_seats = 0
if (is_student and has_pass) or (pay_on_site and free_seats > 0) or (is_guest and current_hour < 20):
    print("Добро пожаловать!")
else:
    print("Доступ запрещён")

#Гости организатора (is_guest = True) теперь должны быть старше 18 лет (age >= 18), и время визита — до 20:00.
is_student = False
has_pass = False
is_guest = True
age = 17  # Добавили возраст
current_hour = 19
pay_on_site = False
free_seats = 5
if (is_student and has_pass) or (pay_on_site and free_seats > 0) or (is_guest and current_hour < 20 and age >= 18):
    print("Добро пожаловать!")
else:
    print("Доступ запрещён")

#Задача 12 (Тернарный оператор).Упрости код, который определяет, можно ли посещать бар. Правило:
#Если возраст (age) ≥ 18, выводи "Добро пожаловать!",
#Иначе — "Вход запрещён".
age = 15
result = "Добро пожаловать!" if (age) >= 18 else "Вход запрещён"
print(result)


# Вот задача 13 на тернарный оператор + логические условия
# Стриминговый сервис предлагает скидку, если пользователь:
#Подписан (is_subscriber = True) И
#Имеет более 1000 просмотров (views > 1000).
#Напиши код, который выводит:
#"Скидка 20%", если условия выполнены,
#"Условия не выполнены" — в остальных случаях.
is_subscriber = True
views = 900
print("Скидка 20%" if is_subscriber and views > 1000 else "Условия не выполнены")

# Вот задача 14 на комбинирование тернарного оператора и сравнений — теперь с тремя вариантами исходов.
# Система оценки температуры кофе:
# Если temp > 75 → "Горячий",
# Если temp < 50 → "Холодный",
# Иначе → "Тёплый".
# Задание:
# Напиши одну строку с тернарным оператором, которая присваивает переменной status соответствующую строку.
temp = 10
status = "Горячий" if temp > 75 else "Холодный" if temp < 50 else "Тёплый"
print(status)

# Задача 15:
# Напиши одну строку с тернарным оператором, которая присваивает переменной quality значение:
# "Отлично", если скорость (speed) > 100 Мбит/с,
# "Нормально", если скорость от 50 до 100 Мбит/с (включительно),
# "Медленно" во всех остальных случаях.
speed = 300
quality = "Отлично" if (speed) > 100 else "Нормально" if 50<= speed <= 100 else "Медленно"
print(quality)

# Задача 16 (Тернарный оператор + проверка типа + сложные условия)
# Напиши одну строку, которая:
# Проверяет, что speed — целое число (isinstance(speed, int)),
# Если это не число, выводит "Ошибка данных",
# Если число, оценивает скорость по правилам:
# > 200 → "Супер!",
# 100-200 → "Отлично",
# 50-99 → "Нормально",
# < 50 → "Медленно".
speed = 200
quality = "Ошибка данных" if not isinstance(speed, int) else "Супер!" if int(speed) > 200 else "Отлично!" if 200 <= int(speed) <= 100 else "Нормально!" if 99 <= int(speed) < 50 else "Медленно"
print(quality)

# Вот похожая задача 17 с тернарным оператором и проверкой типов, но теперь для системы оценки уровня заряда батареи.
# Условие
# Напиши одну строку с тернарным оператором, которая:
# Проверяет, что battery_level — целое число (isinstance(battery_level, int)),
# Если это не число, присваивает переменной status значение "Ошибка данных",
# Если число, оценивает заряд по правилам:
# ≥ 80 → "Высокий",
# 50-79 → "Средний",
# 20-49 → "Низкий",
# < 20 → "Критический".
battery_level = 10
status = "Ошибка данных" if not isinstance(battery_level, int) else "Высокий" if battery_level >= 80 else "Средний" if 79<= battery_level <= 50 else "Низкий" if 49 <= battery_level <= 20 else "Критический"
print(status)

# Вот задача 18 — теперь с комбинацией тернарного оператора, проверки типов и логических операторов.
# Условие
# Система проверки доступа в клуб:
# Если гость указал возраст строкой (например, "25"), вывести "Возраст должен быть числом",
# Если возраст < 18, вывести "Доступ запрещён",
# Если возраст 18-21, вывести "Доступ с ограничениями",
# Если возраст > 21, вывести "Добро пожаловать!".
# Напиши одну строку с тернарным оператором, которая присваивает результат переменной access.
age = "25"  # Обратите внимание: это строка!
access = "Возраст должен быть числом" if isinstance(age, str) else "Доступ запрещён" if age < 18 else "Доступ с ограничениями" if  18 <= age <= 21 else "Добро пожаловать!"
print(access)

# Задача 1 (оператор if)
# Дана переменная score = 75. Напиши код, который выводит "Проходной балл", если значение score больше или равно 60.
score = 75
if score >= 60:
    print("Проходной балл")

# Задача 2 (оператор else)
# Дана переменная speed = 45. Напиши код, который:
# Выводит "Скорость нормальная", если speed <= 60,
# Иначе выводит "Превышение!".
speed = 85
print("Скорость нормальная" if speed <= 60 else "Превышение!")

# Задача 3 (оператор elif)
# Дана переменная grade = "B". Напиши код, который:
# Если grade == "A", выводит "Отлично",
# Если grade == "B", выводит "Хорошо",
# Иначе выводит "Нужно подтянуть".
grade = "5"
if grade == "A":
    print("Отлично")
elif grade == "B":
    print("Хорошо")
else:
    print("Нужно подтянуть")

# Задача 4 (логические операции and/or)
is_weekend = True
is_holiday = False
# Напиши условие, которое выводит "Выходной", если день является выходным (is_weekend) или праздником (is_holiday).
result = "Выходной" if is_weekend or is_holiday else "Work"
print(result)

# Задача 5 (тернарный оператор)
# Дана переменная age = 17. Напиши одну строку, которая присваивает переменной access значение:
# "Доступ разрешён", если age >= 18,
# "Доступ запрещён" — иначе.
age = 19
print("Доступ разрешён" if age >= 18 else "Доступ запрещён")

# Задача 6 (комбинирование условий)
# Напиши условие, которое выводит "Можно лететь", если у человека есть билет (has_ticket)
# И паспорт (has_passport), иначе — "Нельзя лететь".
has_ticket = True
has_passport = False
print("Можно лететь" if has_ticket and has_passport else "Нельзя лететь")

# Задача 7 (проверка диапазона)
# Дана переменная battery = 35. Напиши код, который:
# Выводит "Заряд высокий", если battery >= 70,
# Выводит "Заряд средний", если 30 <= battery < 70,
# Иначе выводит "Заряд низкий".
battery = 35
print("Заряд высокий" if battery >= 70 else "Заряд средний" if 30 <= battery < 70 else "Заряд низкий")

# Задача 8 (комплексная проверка)
is_member = True
total_purchase = 12000
# Напиши условие, которое:
# Дает скидку 10% ("Скидка 10%"), если:
# Покупатель является участником программы (is_member) И
# Сумма покупок (total_purchase) > 10 000,
# Иначе выводит "Скидка 0%".
print("Скидка 10%" if is_member and total_purchase > 10000 else "Скидка 0%" )

if is_member and total_purchase > 10000:
    print("Скидка 10%")
else:
    print("Скидка 0%")


# Задача 2 (Операции сравнения)
a = 10
b = 7
# Напиши код, который выводит на экран результаты следующих операций сравнения (каждый результат с новой строки):
# a больше b
# a меньше или равно b
# a не равно b
print(a > b)
print(a <= b)
print(a != b)

# Задача 3 (Срезы списков)
numbers = [10, 20, 30, 40, 50, 60, 70]
# Выведи следующие элементы, используя срезы (каждый результат с новой строки):
# Первые 3 элемента
# Последние 2 элемента
# Каждый второй элемент, начиная с индекса 1 (т.е. [20, 40, 60])
print(numbers[:3])
print(numbers[-2:])
print(numbers[1::2])

# Задача 4 (F-строки и конкатенация)
name = "Алексей"
age = 28
city = "Москва"
# Создай строку, которая объединит эти данные в следующем формате:
# "Меня зовут Алексей, мне 28 лет, я живу в Москве."
result = f"Меня зовут ", name, "мне ", age, "лет, я живу в", city
print(result)

